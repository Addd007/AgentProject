-- PostgreSQL initialization script for local and production Docker setups.
-- Tables are created by SQLAlchemy at app startup; this file ensures the bind mount exists
-- and can be extended later for roles, extensions, or seed data.

SELECT 1;